export interface Item {
  readonly id: string;
  // title: string;
  // description: string;
  // price: number;
  // amount: number;
}

// export function createItem({ id, title, description, amount }): Item {
//   return { id, title, description, amount };
// }

// export function createItems(data: any[]): Item[] {
//   return data.map(createItem);
// }
